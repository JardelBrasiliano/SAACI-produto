---
description: Gera a descrição de Pull Request no template deste repositório, a partir do diff da branch atual
argument-hint: [branch-alvo]
---

Use a skill `saaci-mr` para gerar a descrição de Pull Request da branch atual contra
`$ARGUMENTS` (ou `main`, se vazio), seguindo todas as regras dessa skill.
